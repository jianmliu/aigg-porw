[package]
name = "aigg-porw-core"
description = "Chain-neutral Proof-of-Resident-Weights (PoRW) research primitives and reference verification"
license = "0BSD"
version = "0.1.0"
authors = ["Jianming Liu <liujm06@gmail.com>"]
edition.workspace = true
repository = "https://github.com/jianmliu/aigg-porw"
readme = "README.md"
include = [
    "/src",
    "/tests",
    "/Cargo.toml",
    "/LICENSE-0BSD",
    "/README.md",
]

[lib]
bench = false

[[test]]
name = "spec_lock"
path = "tests/spec_lock.rs"
required-features = ["repository-conformance"]

[dependencies]
blake3.workspace = true
parity-scale-codec = { workspace = true, features = ["derive"], optional = true }
scale-info = { workspace = true, features = ["derive"], optional = true }

[dev-dependencies]
serde_json = "=1.0.145"
sha2 = "=0.10.9"

[features]
default = ["std"]
std = [
    "blake3/std",
    "parity-scale-codec?/std",
    "scale-info?/std",
]
scale = ["dep:parity-scale-codec", "dep:scale-info"]
repository-conformance = []
